spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/notification_db_dev
  
  kafka:
    bootstrap-servers: localhost:9092
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:your-email@gmail.com}
    password: ${MAIL_PASSWORD:your-app-password}

logging:
  level:
    com.jirathon: DEBUG
    org.springframework.kafka: DEBUG
